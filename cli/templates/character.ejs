---
title: <%= title %>
sidebar:
  badge: Stub
extraMetadata:
  ddb: <%= extraMetadata.ddb %>
  image:
    url: <%= extraMetadata.image.url %>
    attribution: <%= extraMetadata.image.attribution %>
    attribution_url: <%= extraMetadata.image.attribution_url %>
  character:
    aliases:
    <%_ extraMetadata.character.aliases.forEach(function(alias){ _%>
    - <%- alias %>
    <%_ }) _%>
    age: <%= extraMetadata.character.age %>
    date_of_birth: <%= extraMetadata.character.date_of_birth %>
    date_of_death: <%= extraMetadata.character.date_of_death %>
    sex: <%= extraMetadata.character.sex %>
    pronouns: <%= extraMetadata.character.pronouns %>
    height: <%= extraMetadata.character.height %>
    weight: <%= extraMetadata.character.weight %>
    
tags:
  <%_ tags.forEach(function(tag){ _%>
  - <%- tag %>
  <%_ }) _%>
---

<Stub />
